<Alloy>

	<NavigationGroup id="navgroup">
		<Window id="win" title="Listagem dos ônibus">
			<TableView id="table" dataCollection="onibusCollection" onClick="loadDetails" class="container" filterAttribute="nome">
				<SearchBar platform="android,ios"/>
				<TableViewRow class="row" modelId="{id_onibus}">
					<View class="rowContainer">
						<Label class="title" text="{nome}"/>
						<Label class="subtitle" text="{numero}"/>
						<Label class="subtitle" text="{sentido}"/>
					</View>
				</TableViewRow>
			</TableView>
		</Window>
	</NavigationGroup>

</Alloy>